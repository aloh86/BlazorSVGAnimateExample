@page "/"

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

<div>
    This rect will animate on page load.
</div>
<svg>
    <rect width="100" height="100">
        <animate attributeName="width" from="100" to="50" dur="1s" fill="freeze" repeatCount="1" begin="0s" />
    </rect>
</svg>


<div>
    We want animation to happen after we change the "to" attribute value, but nothing happens. Begin attribute is set to "0s" (start immediately).
</div>
<div>
    <button @onclick="() => ToVal = 50">Change value</button>
</div>
<svg>
    <rect width="100" height="100">
        <animate attributeName="width" from="100" to="@ToVal" dur="1s" fill="freeze" repeatCount="2" begin="0s" />
    </rect>
</svg>

<div>
    We want animation to happen after we change the "to" attribute value after we click on the square. Same as above, but begin attribute is set to "click". Works ok.
</div>
<svg>
    <rect width="100" height="100">
        <animate attributeName="width" from="100" to="50" dur="1s" fill="freeze" repeatCount="1" begin="click" />
    </rect>
</svg>

@code {
    public int ToVal { get; set; }

    public Index()
    {
        ToVal = 100;
    }
} 